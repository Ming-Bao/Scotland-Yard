Things I disgarded during testing.
- using api for routing is infeasable because there is too many edges between nodes and the cost would be too high.


Thinkg that I learned dring testing
- Performance turned out to be important
    - Apis are again infeasable because its too slow
    - Even without apis and having pre computed paths. Rendering the nodes and edges in the browser can be laggy and needs to be look into.


Initially wanted to go with the api routing path but I found geojson and that was a much better option than apis becaues its free and I can pre compute paths and store them.
